<div class="my-3 py-3">
    <a href="{{ path('product', { 'slug' : product.slug }) }}" style="text-decoration: none; color: black;">
        <img src="/uploads/{{ product.illustration }}" alt="{{ product.name }}" class="img-fluid" style="height: 300px; border-radius: 10px;">
        <h4 class="display-6">{{ product.name }}</h4>
        <p class="lead">{{ product.subtitle }}</p>
        <span>{{ (product.price / 100)|number_format(2, ',', '.') }} €</span>
    </a>
</div>
